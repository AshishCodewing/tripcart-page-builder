// Preview taxonomy stub (next-wp parity). Authors aren't modeled yet —
// placeholder so the route resolves; build out when authors land.
export default function AuthorsPreview() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-12">
      <h1 className="text-3xl font-semibold">Authors</h1>
      <p className="mt-2 text-muted-foreground">Not implemented yet.</p>
    </main>
  )
}
