// Preview taxonomy stub (next-wp parity). Tag archive listing —
// placeholder so the route resolves; build out when archives land.
export default function TagsPreview() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-12">
      <h1 className="text-3xl font-semibold">Tags</h1>
      <p className="mt-2 text-muted-foreground">Not implemented yet.</p>
    </main>
  )
}
