# Implementation Plans

Generated by the improve skill on 2026-06-11 against commit `ae527df`;
plans 018–022 added by a follow-up audit on 2026-07-08 against commit
`9f96f3b` (focused on the AI copilot / billing / ledger surface shipped
since June). Execute in the order below unless dependencies say otherwise.
Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Verification baseline — Vitest + characterization tests | P1 | M | — | DONE |
| 002 | GitHub Actions CI gate (typecheck/lint/test) | P1 | S | 001 (test step) | DONE |
| 003 | Cancel pending autosave debounce on explicit save | P2 | S | — | DONE |
| 004 | Validate project JSON payloads at the action boundary | P2 | M | 001 | DONE |
| 005 | Theme schema single source of truth (Zod-derived types) | P3 | M | 001 | DONE |
| 006 | RAG side-project decoupling — investigate & recommend | P3 | M | — | DONE (memo: docs/rag-decoupling-decision.md — recommends (b), fold into 007) |
| 007 | Repo hygiene batch (README, strays, deps, env docs, guard) | P3 | S | — | DONE (strays pre-deleted in 626459f; includes 006's RAG-dep move) |
| 008 | LAYOUT chrome ownership — Approach A (site/route owns frame, §14) | P2 | M | 001 | REVERTED (2026-06-15: Approach A — `layoutSlug` zones + `proxy.ts` region routing + `[zone]` layouts + `content-slot` — was built then fully reverted. Replaced by tenant-assigned header/footer templates (`Tenant.headerTemplateId`/`footerTemplateId`) rendered in `app/preview/[tenantId]/layout.tsx`; preview routes now next-wp-style `pages/[...slug]` + `posts/[slug]`. See §14 / layout-render-fork.md reversal notes. Plan retained for history) |
| 009 | Public render path — design spike | P2 | M | — (read 008) | DONE — BUILD DEFERRED (2026-06-16: scope is the page builder, not serving the live site; design memo stands for whenever it's greenlit: docs/public-render-design.md — operator confirmed no separate renderer exists; recommends Option A (public routes in this repo, host→tenant middleware, two-level Cache Components tags). Flags pre-req fixes: tenant-scope page/post cache tags, resolver `touchedSlugs` return shape, enable `cacheComponents`) |
| 010 | Synced-template per-instance overrides (§11) — design spike | P3 | M | after 008 ideally | DONE — BUILD DEFERRED (2026-06-16: maintainer parked the build; synced/unsynced modes suffice for now. Design memo stands for whenever it's greenlit: docs/template-overrides-design.md — binding decl `data-tc-binding`(+kind/attr) on template children, `data-overrides` JSON on the ref (persists free via existing `toJSON`); resolver applies via a scoped `applyBindings` walk that stops at nested refs; v1 kinds text+attribute (rich deferred); ships v0 form-based override panel before v1 in-canvas. No `Template.data`/payload-schema change needed) |
| 011 | Characterization tests for react-renderer pure modules | P2 | M | 001 | DONE |
| 012 | Public render path — build (host-routed published pages) | P2 | L | 009 (design), 008 (shipped chrome) | DEFERRED (2026-06-16: scope is the page builder, not serving the live site. Plan stands ready to execute whenever public rendering is greenlit) |
| 013 | User-authored single-post templates (content-slot + dynamic fields) | P2 | L | multi-header Pieces 1–2 (shipped) | NOT STARTED (2026-06-16: "Option C" design approved; reserved `single` LAYOUT slug + server-side post-field binding. Phased: data+binding+render → editor authoring → assignment/per-post) |
| 014 | Chrome assignment UI (multi-header Piece 3) | P2 | M | Pieces 1–2 (shipped) | NOT STARTED (2026-06-16: per-segment header/footer Select panel over the shipped `ChromeAssignment` model; extend rename guard. No migration) |
| 015 | Credit ledger foundation (generic double-entry core) | P2 | L | — (additive) | NOT STARTED (2026-06-26: generic ledger only — accounts/transactions/entries/balance projection + `LedgerService.postTransaction`. Composition over inheritance; `bigint` units; `FOR UPDATE` concurrency; idempotent replay. Business services out of scope. Design: docs/reference/ledger.md) |
| 016 | Copilot prompt & editor-context overhaul | P2 | M (Ph 1–3) / L (Ph 4) | — (additive; branch `feat/ai-assistant`) | Ph 1–3 DONE (2026-07-06: v2 chat-first prompt live in Langfuse; `getProjectData()` → exported HTML/CSS context, −39% payload; history capped at 10; `availablePages` dropped — one project = one page; verified grounding/selection/refusal via traces. Finding: minimax-m3 ignores cache_control). Ph 4 → plan 017 |
| 017 | Copilot tools & code-gen orchestration (016 Ph 4) | P2 | L | 016 Ph 1–3 (shipped) | DONE (2026-07-06: two-tier shipped + verified E2E — gpt-5-mini orchestrator + gpt-5.1 codegen via `/api/generate`; 5 client tools, approval on edit/remove, single-undo apply, real implicit-cache hits; prompts live in Langfuse — codegen v1 + copilot v3, trace-verified) |
| 018 | Unit tests — billing gate/middleware + AI prompt builders | P1 | S | — (land BEFORE 019/020) | DONE (2026-07-10, branch `advisor/018-billing-ai-unit-tests`: added `gate.test.ts` (6), `copilot.test.ts` (5), `codegen.test.ts` (18); `usage-middleware.test.ts` already existed from the reported-cost feature — augmented with the 4 missing plan cases (zero-usage no-charge, onAbort/onError settle, undefined-token NaN guard, `settledWithTimeout` fake-timers). DRIFT: `usage-middleware.ts` gained `flush()`+reported-cost since planning; tests pin current behavior. Suite 351 pass; no production code changed) |
| 019 | AI route correctness — abort propagation, error-path settle, timer, context dedupe | P1 | S | 018 | DONE (2026-07-10, branch `advisor/019-ai-route-correctness`: (1) `request.signal`→`abortController.abort()` in both routes; passed `abortController` into generate's `chat()` (typecheck confirms the option is accepted); (2) moved generate's `after()` before the `try` so the error path still settles charges + flushes spans; (3) `settledWithTimeout` now `clearTimeout`s on settle; (4) copilot tier-2 caps selected-component HTML at 4000 chars (id-reference beyond). DRIFT: generate route uses `chargeFlushes: (()=>Promise)[]` (flush-fn pattern from reported-cost) not the plan's `settledCharges` — fix applied to the live shape. Updated 018 tests (timer-clear + oversize-selection cases). Suite 354 pass; typecheck/lint clean. Route abort/after() not unit-testable — covered by greps + typecheck) |
| 020 | Billing integrity hardening — chat body validation, tenant-resolution seam, gate policy flag | P1 | M | 018; shares files with 019 | TODO |
| 021 | Ledger integration tests — postTransaction DB semantics in CI | P2 | M | — | DONE (2026-07-10, branch `advisor/021-ledger-integration-tests`: `lib/ledger/ledger.integration.test.ts` (7 cases: grant+projection, idempotent replay, concurrent-same-key→1 row, overspend rollback, system-account-negative, unknown-account, projection=SUM(entries)); split configs (`vitest.config.ts` excludes `*.integration.test.ts`; new `vitest.integration.config.ts`, `fileParallelism:false`, dotenv setup) + `test:integration` script; CI gains a `pgvector/pgvector:pg16` service (RAG init migration CREATEs the vector ext) + `migrate deploy` + `test:integration`. VERIFIED locally: ran twice consecutively against the local DB → 7/7 both times (cleanup leaves no residue); default `pnpm test` 320 pass with integration excluded; typecheck/lint clean; YAML valid. No production ledger code touched; `scripts/smoke-ledger.ts` retained) |
| 022 | Admin credit grant/remove — replenishment phase 1 | P2 | S–M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **001 first.** Plans 004, 005, and 008 all modify code that 001's
  characterization tests pin down (`templates.ts`, the theme schema, save
  actions). Executing them before 001 forfeits the safety net.
- 002's `pnpm test` step assumes 001's `test` script exists; 002 documents
  the fallback if executed first.
- 003 and 007 are independent — safe to run anytime, in parallel with others.
- 006, 009, 010 are read-only investigations producing docs — safe anytime;
  010 should be *designed against* the post-008 resolver, so run it after
  008 (or note the divergence).
- 004 and 008 both touch `lib/cms/page-actions.ts` — coordinate merge order
  (either order works; second one rebases).
- 011 requires 001's Vitest setup (config + `test` script); it adds test
  files only. Its render tests double as the output contract for 009.
- **018 before 019 and 020**: both modify code 018's tests pin (gate,
  usage-middleware, prompt builders); their plans say which pinned cases
  they deliberately change.
- 019 and 020 both touch `app/api/chat/route.ts` and
  `app/api/generate/route.ts` — either order works; second one rebases.
- 021 and 022 are independent of everything; 022 optionally adds one case
  to 021's harness if it exists.

## Known but unplanned (maintainer's explicit choice — 2026-06-11)

These were the top findings by leverage; the maintainer chose not to plan
them this run. Recording them so they aren't lost and aren't re-audited as
new discoveries:

1. **No authentication/authorization anywhere** — admin routes, all five
   server-action modules, and draft-mode enable are unguarded; tenant
   isolation unenforced (id-only lookups). Known TODOs at
   `lib/cms/editor-draft-actions.ts:22-23` and `app/api/preview/route.ts:14-17`.
   The app is deployed on Vercel. When auth is planned, the IDOR dimension
   (tenant-scoping every id-based lookup) must be designed in, not bolted on.
2. **Next.js 16.1.7 carries 11 high-severity advisories** — patched in
   ≥ 16.2.5 (`pnpm audit --prod`). The upgrade is small and should not wait
   long.
3. **Secrets burned in git history** — `.env` was committed (added in
   `a3c45f8`/`fba2a40`, removed in `6abd7e1`); removal is not rotation. The
   database password and Google API key in history should be rotated.

### From the react-renderer focused audit (2026-06-11, same commit)

The maintainer planned only the test baseline (plan 011) from this run.
Findings 4–8 were subsequently fixed; finding 9 stays deferred.

4. **Project renderer coerces numeric-looking string props to numbers** —
   `lib/plugins/react-renderer/project/render-component.tsx`.
   **Fixed 2026-06 — advisor/012-react-renderer-small-fixes**: coercion is
   now round-trip-safe (`String(Number(v)) === v`), so `"01234"` stays a
   string while clean integers still coerce.
5. **Unregistered function components silently degrade** —
   `lib/plugins/react-renderer/process.ts`.
   **Fixed 2026-06 — advisor/012-react-renderer-small-fixes**: unregistered
   components now `console.warn` and drop the `type` key (no
   `type:undefined`); Fragments flatten transparently. Accepted limitation:
   a zero- or multi-child Fragment at the **processor root** still
   materializes a default container, because GrapesJS shallow-merges the
   processor result as a single object and cannot accept an array there.
6. **`isComponent` never matches** —
   `lib/plugins/react-renderer/register.ts`.
   **Fixed 2026-06 — advisor/012-react-renderer-small-fixes**: tag names
   now compare case-insensitively, so HTML paste/import recognizes React
   component types (cautioned: the recognizer is now live).
7. **Raw `view.el = el` rebinding skips Backbone `setElement`** —
   `lib/plugins/react-renderer/bind.ts`.
   **Fixed 2026-06 — advisor/012-react-renderer-small-fixes**: the
   existing-view path now calls `view.setElement(el)` so `$el` and
   delegated handlers re-wire. Gated by the manual canvas checklist (no
   automated coverage).
8. **Small debt batch** — cross-frame `dropView`, `console.error` noise on
   plain style strings, dead conditions in `attrs.ts` / `process.ts`, and a
   missing "pinned GrapesJS internals" doc note.
   **Fixed 2026-06 — advisor/012-react-renderer-small-fixes**: `dropView`
   is frame-scoped, the style `console.error` is gone, the dead conditions
   are removed, and `docs/reference/rendering-pipeline.md` now has a "Pinned
   GrapesJS internals (check on upgrade)" section.
9. **`DataSourceManager` constructed but never consumed**
   (`project/project-editor.ts:16`) — decide keep-as-seam vs delete when
   plan 009 runs. **Still deferred to plan 009.**

## Findings considered and rejected (so nobody re-audits them)

- **Render-root robustness (catch-and-continue / null deref in
  `lib/plugins/react-renderer/render-root.tsx`)**: homegrown port of the
  Studio SDK renderer; React render errors flow through the existing
  RenderError path; low real-world value.
- **Autosave cross-record corruption (stale `persistDraft` after
  navigation)**: not reproducible — per-instance refs, unmount flush, and
  `key={storageKey}` remount cover it. (The narrower publish-race IS real →
  plan 003.)
- **`listPages` over-fetching/pagination**: wrong — it selects slim columns
  (no `data`); pagination unneeded at current per-tenant scale.
- **Theme CSS recompiled per request**: by design — the route serves
  `immutable` responses keyed by `themeVersion`; recompiles happen on cache
  miss only.
- **Render-path caching / `buildPath` N+1**: premature — preview is
  draft-mode-dynamic by design; the caching design already exists in
  `docs/reference/templates-followups.md` §1 notes for when the public render lands
  (see plan 009).
- **CSRF on server actions**: Next.js server actions ship origin checking;
  by-design.
- **Theme CSS route publicly readable**: theme CSS must be public for
  published sites; intentional.
- **Editor bundle code-splitting**: the GrapesJS-importing shell is only
  reachable from edit routes; Next route-level splitting already isolates it.
- **GrapesJS 0.22→0.23 / TS 6 / ESLint 10 upgrades**: real but deliberately
  not planned this run (MED-risk editor upgrade with no test coverage yet —
  revisit after 001/002 land).
- **`<style>{css}</style>` escaping in `project/render-page.tsx` /
  `render-project.tsx`**: refuted by experiment 2026-06-11 — React 19
  renders text children of `<style>` raw (`>`, `"`, `&` unescaped via
  `renderToStaticMarkup`); no `dangerouslySetInnerHTML` needed. Plan 011
  pins this with a test.

## Follow-up audit 2026-07-08 (commit `9f96f3b`) — unplanned & rejected

Standard-depth re-audit weighted to code shipped since June (`lib/ai`,
`lib/billing`, `lib/ledger`, `app/api/chat|generate|credits`,
`components/ai`), reconciled against this file. Confirmed resolved from the
June list: Next.js advisories (now 16.2.9), RAG dev-dep isolation (plans
006/007). Still standing: no auth (now escalated — see plan 020's residual-
risk note), secrets-in-history rotation.

**Known but unplanned (maintainer's choice, 2026-07-08):**

1. **Langfuse traces capture full page HTML/CSS + user messages**
   (`lib/ai/tracing.ts:47` `captureContent: true`, no `mask`, no user id) —
   tenant draft content ships to a third-party trace store. Data-governance
   decision: enable a `mask` on the span processor or accept and document.
2. **Codegen context cost** — every edit sends the full page HTML+CSS to the
   expensive model, and `buildCodegenSystemPrompts` orders the volatile task
   section BEFORE the page body (`lib/ai/codegen.ts:176-197`), defeating
   implicit prefix caching across generations; the contract-retry re-sends
   everything (`app/api/generate/route.ts:115-129`). Design-sensitive
   (subtree-only context risks quality); revisit with Langfuse cost data.
3. **Chat transcript re-renders on every composer keystroke** — input state
   at the `Chat` root + unmemoized `MessageView` re-parses all markdown
   (`components/ai/chat.tsx:116`, `components/ai/message-parts.tsx:221-246`).
   S-effort memo fix whenever the panel feels laggy.
4. **Per-turn usage UI built but disabled** — `<UsageLine>` render commented
   out (`components/ai/message-parts.tsx:242`) while its capture pipeline
   still runs (`chat.tsx:156-186`). Confirm intent, then re-enable or delete.
5. **Direction (evidence-grounded, not selected this run):** Stripe top-up
   over `createCreditPurchase` (blocked on `lib/billing/pricing.ts` TODOs);
   AI "save as pattern" into the Library (codegen output = pattern artifact);
   approval-gated theme-token tool (copilot reads tokens but can't write —
   "make the site warmer" impossible today).

**Considered and rejected (2026-07-08, so nobody re-audits them):**

- **Gate fails open** (`lib/billing/gate.ts:38,42`): deliberate, documented
  stance; plan 020 makes it an explicit env-flagged policy rather than
  "fixing" it.
- **Client-side-only tool approval** (`needsApproval` in `lib/ai/tools.ts`):
  tools execute on the requester's own canvas, generated HTML never
  re-enters the orchestrator — no cross-tenant/server boundary crossed.
  Document the trust boundary if server-authoritative mutations ever land.
- **Dust-balance gate/clamp coupling + split idempotency keys**
  (`lib/billing/ai-usage.service.ts:136,164`; `gate.ts:18`): both
  architecture-guarded today (all factories post whole-credit multiples;
  usageId is per-run unique). Latent, not live; noted for the next billing
  change.
- **3 HIGH `pnpm audit --prod` advisories**: all transitive via
  `@prisma/client > prisma > @prisma/dev` (hono/fast-uri) — Prisma's local
  dev server, not the deployed request path. Clears on a routine Prisma bump.
- **`@ai-sdk/openai` "unused" direct dep**: it satisfies
  `@grapesjs/studio-sdk-plugins`' peer range — leave it (comment optional).
- **Form-control dispatch duplication across the two react-renderer halves**:
  in-flight WIP in the working tree at audit time (`form-controls.ts`
  extraction) — the maintainer is actively mid-change there.
- **GrapesJS 0.22→0.23**: still deferred — tests now exist but none cover
  the canvas half the bump would break; needs a Playwright canvas smoke
  harness first (re-recorded from June with updated rationale).
- **CLAUDE.md staleness + `lib/ai` scaffolding dedup + empty `providers.ts`**
  (hygiene batch): real but not selected this run — CLAUDE.md:20 still says
  "no test runner" (false since plan 001) and omits the AI/billing/ledger
  stack; `fetchCopilotPrompt`/`fetchCodegenPrompt` are near-identical copies.
  Cheap to fold into any future hygiene pass.

## Audit scope note

Audited at `standard` depth on 2026-06-11: `lib/cms`, `lib/plugins`,
`lib/theme`, `app/admin`, `app/preview`, `app/api`,
`components/page-builder`, hooks, dependencies, tooling. NOT audited:
`lib/rag` + ingest-script internals (treated as a side project — see plan
006), `components/ui` shadcn primitives, visual/UX quality of the pattern
library.

**Focused follow-up (2026-06-11, commit `ae527df`):** deep line-by-line
audit of `lib/plugins/react-renderer/` (all 21 files, both the canvas and
project halves) plus its consumers (`app/preview/*`, `page-preview.tsx`,
`patterns/index.ts`, editor-shell wiring). Produced plan 011 and the
unplanned findings 4–9 above. Not covered: runtime behavior of the canvas
half inside a live GrapesJS editor (no harness exists).
