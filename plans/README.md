# Implementation Plans

Generated by the improve skill on 2026-06-11 against commit `ae527df`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Verification baseline — Vitest + characterization tests | P1 | M | — | DONE |
| 002 | GitHub Actions CI gate (typecheck/lint/test) | P1 | S | 001 (test step) | TODO |
| 003 | Cancel pending autosave debounce on explicit save | P2 | S | — | TODO |
| 004 | Validate project JSON payloads at the action boundary | P2 | M | 001 | TODO |
| 005 | Theme schema single source of truth (Zod-derived types) | P3 | M | 001 | TODO |
| 006 | RAG side-project decoupling — investigate & recommend | P3 | M | — | TODO |
| 007 | Repo hygiene batch (README, strays, deps, env docs, guard) | P3 | S | — | TODO |
| 008 | LAYOUT content slot + page→layout assignment (§14) | P2 | M | 001 | TODO |
| 009 | Public render path — design spike | P2 | M | — (read 008) | TODO |
| 010 | Synced-template per-instance overrides (§11) — design spike | P3 | M | after 008 ideally | TODO |
| 011 | Characterization tests for react-renderer pure modules | P2 | M | 001 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **001 first.** Plans 004, 005, and 008 all modify code that 001's
  characterization tests pin down (`templates.ts`, the theme schema, save
  actions). Executing them before 001 forfeits the safety net.
- 002's `pnpm test` step assumes 001's `test` script exists; 002 documents
  the fallback if executed first.
- 003 and 007 are independent — safe to run anytime, in parallel with others.
- 006, 009, 010 are read-only investigations producing docs — safe anytime;
  010 should be *designed against* the post-008 resolver, so run it after
  008 (or note the divergence).
- 004 and 008 both touch `lib/cms/page-actions.ts` — coordinate merge order
  (either order works; second one rebases).
- 011 requires 001's Vitest setup (config + `test` script); it adds test
  files only. Its render tests double as the output contract for 009.

## Known but unplanned (maintainer's explicit choice — 2026-06-11)

These were the top findings by leverage; the maintainer chose not to plan
them this run. Recording them so they aren't lost and aren't re-audited as
new discoveries:

1. **No authentication/authorization anywhere** — admin routes, all five
   server-action modules, and draft-mode enable are unguarded; tenant
   isolation unenforced (id-only lookups). Known TODOs at
   `lib/cms/editor-draft-actions.ts:22-23` and `app/api/preview/route.ts:14-17`.
   The app is deployed on Vercel. When auth is planned, the IDOR dimension
   (tenant-scoping every id-based lookup) must be designed in, not bolted on.
2. **Next.js 16.1.7 carries 11 high-severity advisories** — patched in
   ≥ 16.2.5 (`pnpm audit --prod`). The upgrade is small and should not wait
   long.
3. **Secrets burned in git history** — `.env` was committed (added in
   `a3c45f8`/`fba2a40`, removed in `6abd7e1`); removal is not rotation. The
   database password and Google API key in history should be rotated.

### From the react-renderer focused audit (2026-06-11, same commit)

The maintainer planned only the test baseline (plan 011) from this run.
Findings 4–8 were subsequently fixed; finding 9 stays deferred.

4. **Project renderer coerces numeric-looking string props to numbers** —
   `lib/plugins/react-renderer/project/render-component.tsx`.
   **Fixed 2026-06 — advisor/012-react-renderer-small-fixes**: coercion is
   now round-trip-safe (`String(Number(v)) === v`), so `"01234"` stays a
   string while clean integers still coerce.
5. **Unregistered function components silently degrade** —
   `lib/plugins/react-renderer/process.ts`.
   **Fixed 2026-06 — advisor/012-react-renderer-small-fixes**: unregistered
   components now `console.warn` and drop the `type` key (no
   `type:undefined`); Fragments flatten transparently. Accepted limitation:
   a zero- or multi-child Fragment at the **processor root** still
   materializes a default container, because GrapesJS shallow-merges the
   processor result as a single object and cannot accept an array there.
6. **`isComponent` never matches** —
   `lib/plugins/react-renderer/register.ts`.
   **Fixed 2026-06 — advisor/012-react-renderer-small-fixes**: tag names
   now compare case-insensitively, so HTML paste/import recognizes React
   component types (cautioned: the recognizer is now live).
7. **Raw `view.el = el` rebinding skips Backbone `setElement`** —
   `lib/plugins/react-renderer/bind.ts`.
   **Fixed 2026-06 — advisor/012-react-renderer-small-fixes**: the
   existing-view path now calls `view.setElement(el)` so `$el` and
   delegated handlers re-wire. Gated by the manual canvas checklist (no
   automated coverage).
8. **Small debt batch** — cross-frame `dropView`, `console.error` noise on
   plain style strings, dead conditions in `attrs.ts` / `process.ts`, and a
   missing "pinned GrapesJS internals" doc note.
   **Fixed 2026-06 — advisor/012-react-renderer-small-fixes**: `dropView`
   is frame-scoped, the style `console.error` is gone, the dead conditions
   are removed, and `docs/reference/rendering-pipeline.md` now has a "Pinned
   GrapesJS internals (check on upgrade)" section.
9. **`DataSourceManager` constructed but never consumed**
   (`project/project-editor.ts:16`) — decide keep-as-seam vs delete when
   plan 009 runs. **Still deferred to plan 009.**

## Findings considered and rejected (so nobody re-audits them)

- **Render-root robustness (catch-and-continue / null deref in
  `lib/plugins/react-renderer/render-root.tsx`)**: homegrown port of the
  Studio SDK renderer; React render errors flow through the existing
  RenderError path; low real-world value.
- **Autosave cross-record corruption (stale `persistDraft` after
  navigation)**: not reproducible — per-instance refs, unmount flush, and
  `key={storageKey}` remount cover it. (The narrower publish-race IS real →
  plan 003.)
- **`listPages` over-fetching/pagination**: wrong — it selects slim columns
  (no `data`); pagination unneeded at current per-tenant scale.
- **Theme CSS recompiled per request**: by design — the route serves
  `immutable` responses keyed by `themeVersion`; recompiles happen on cache
  miss only.
- **Render-path caching / `buildPath` N+1**: premature — preview is
  draft-mode-dynamic by design; the caching design already exists in
  `docs/reference/templates-followups.md` §1 notes for when the public render lands
  (see plan 009).
- **CSRF on server actions**: Next.js server actions ship origin checking;
  by-design.
- **Theme CSS route publicly readable**: theme CSS must be public for
  published sites; intentional.
- **Editor bundle code-splitting**: the GrapesJS-importing shell is only
  reachable from edit routes; Next route-level splitting already isolates it.
- **GrapesJS 0.22→0.23 / TS 6 / ESLint 10 upgrades**: real but deliberately
  not planned this run (MED-risk editor upgrade with no test coverage yet —
  revisit after 001/002 land).
- **`<style>{css}</style>` escaping in `project/render-page.tsx` /
  `render-project.tsx`**: refuted by experiment 2026-06-11 — React 19
  renders text children of `<style>` raw (`>`, `"`, `&` unescaped via
  `renderToStaticMarkup`); no `dangerouslySetInnerHTML` needed. Plan 011
  pins this with a test.

## Audit scope note

Audited at `standard` depth on 2026-06-11: `lib/cms`, `lib/plugins`,
`lib/theme`, `app/admin`, `app/preview`, `app/api`,
`components/page-builder`, hooks, dependencies, tooling. NOT audited:
`lib/rag` + ingest-script internals (treated as a side project — see plan
006), `components/ui` shadcn primitives, visual/UX quality of the pattern
library.

**Focused follow-up (2026-06-11, commit `ae527df`):** deep line-by-line
audit of `lib/plugins/react-renderer/` (all 21 files, both the canvas and
project halves) plus its consumers (`app/preview/*`, `page-preview.tsx`,
`patterns/index.ts`, editor-shell wiring). Produced plan 011 and the
unplanned findings 4–9 above. Not covered: runtime behavior of the canvas
half inside a live GrapesJS editor (no harness exists).
